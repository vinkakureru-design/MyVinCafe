protected override void OnModelCreating(ModelBuilder modelBuilder)
{
    base.OnModelCreating(modelBuilder);

    // Otomatis buat data Admin saat migration
    modelBuilder.Entity<User>().HasData(new User
    {
        Id = 1,
        FullName = "Administrator Vinka",
        Username = "admin",
        Password = "123", // Saran: Gunakan password yang lebih kuat nanti
        Role = "Admin"
    });
}